{
  "name": "csc-307-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "frontend": "npm -w packages/react-frontend run dev",
    "backend": "npm -w packages/express-backend run dev",
    "macdev": "concurrently \"npm -w packages/express-backend run dev\" \"npm -w packages/react-frontend run dev\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "nodemon": "^3.1.9"
  },
  "dependencies": {
    "concurrently": "^9.1.2",
    "jest": "^1.0.0"
  }
}
